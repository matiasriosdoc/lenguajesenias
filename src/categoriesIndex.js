import {
  games,
  education,
  alphabet,
  sexualEducation,
  calendar,
  camp,
  cpcd,
  body,
  birthday,
  hospital,
  house,
  school,
  family,
  beach,
  clothes,
  technology,
  differences,
  professions,
  food,
  animals,
  colors,
  numbers,
  feelings,
  walking,
  introducing,
  where,
  time,
  sports,
  measurements,
  associations,
  geography,
} from './categories';

const uri = 'https://lsa-argentina-videos.s3-sa-east-1.amazonaws.com';
export default {
  categories: [
    alphabet(uri),
    sexualEducation(uri),
    calendar(uri),
    camp(uri),
    body(uri),
    cpcd(uri),
    birthday(uri),
    hospital(uri),
    house(uri),
    school(uri),
    family(uri),
    beach(uri),
    clothes(uri),
    technology(uri),
    differences(uri),
    professions(uri),
    food(uri),
    animals(uri),
    colors(uri),
    numbers(uri),
    feelings(uri),
    walking(uri),
    introducing(uri),
    where(uri),
    time(uri),
    sports(uri),
    education(uri),
    games(uri),
    measurements(uri),
    associations(uri),
    geography(uri),
  ],
};
